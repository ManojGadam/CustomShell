/*Checks whether the provided input command is valid or not. Can add valid inputs to array to support them
@param cmd is the input command 
@return if valid retuns 1 otherwise 0
*/
int isValidInput(char *cmd);
